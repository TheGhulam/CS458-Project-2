The tests are done on Pixel 8 Pro with Android 13

To run Appium:
npm install -g appium
npm install -g appium-doctor
appium-doctor // check appium configuration
appium driver install uiautomator2
appium // run appium

Once Appium is running in a command line, you can run the test from Android Studio.